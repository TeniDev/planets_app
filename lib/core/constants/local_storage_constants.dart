/// [LocalStorageConstants] is a class which stores the local storage keys.
class LocalStorageConstants {
  /// [sessionBox] is a key for storing the session data in the local storage.
  static const String sessionBox = 'sessionBox';

  /// [localeKey] is a key for storing the locale data in the local storage.
  static const String localeKey = 'locale';

  /// [darkModeKey] is a key for storing if the dark mode is enabled in the local storage.
  static const String darkModeKey = 'isDarkMode';

  /// [planetsBox] is a key for storing the planets data in the local storage.
  static const String planetsBox = 'planetsBox';

  /// [favoritesPlanetsKey] is a key for storing the favorite planets data in the local storage.
  static const String favoritePlanetsKey = 'favoritePlanets';
}
